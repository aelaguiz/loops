{
  "sourceUrl": "https://loops.elorm.xyz/loops/flaky-test-triage",
  "downloadedCompatibleBundlePath": ".cursor/loops/flaky-test-triage/",
  "effectiveGuardrails": [
    "Do not modify the check command or exit criteria to force success",
    "Do not skip, disable, or bypass checks to pass the exit condition",
    "If stuck after several iterations, stop and report blockers instead of gaming metrics",
    "Do not weaken, delete, or skip tests to make the suite pass",
    "Do not replace real assertions with trivial always-pass tests",
    "Prefer fixing production code over patching tests to go green"
  ],
  "kickoffWithGuardrails": "Start the \"Flaky Test Triage\" loop.\n\nGoal: classify failing tests as flaky vs real and fix only real regressions\nMax iterations: 5\nBetween iterations run: npm test -- --testPathPattern=<failing-suite>\nExit when: every failure is classified and real regressions are fixed or explicitly deferred\n\nStep 1: Run the failing suite multiple times. Classify each failure, fix real ones, and document flaky behavior.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.\n\nGuardrails (do not skip):\n- Do not modify the check command or exit criteria to force success\n- Do not skip, disable, or bypass checks to pass the exit condition\n- If stuck after several iterations, stop and report blockers instead of gaming metrics\n- Do not weaken, delete, or skip tests to make the suite pass\n- Do not replace real assertions with trivial always-pass tests\n- Prefer fixing production code over patching tests to go green",
  "loop": {
    "id": "loopa2b3a68a4faa8bcad62793ba",
    "slug": "flaky-test-triage",
    "title": "Flaky Test Triage",
    "description": "Run failing tests repeatedly, classify each failure as flaky or real, and fix only confirmed regressions.",
    "category": "Testing",
    "triggerType": "manual",
    "triggerConfig": {
      "type": "manual"
    },
    "steps": [
      {
        "name": "Run failing tests",
        "prompt": "Run the failing test file or suite 3–5 times. Record pass/fail pattern per test.",
        "shellCheck": "npm test -- --testPathPattern=<failing-suite>"
      },
      {
        "name": "Classify failures",
        "prompt": "Label each failure as flaky (intermittent) or real (consistent). Note timing, ordering, or env dependencies."
      },
      {
        "name": "Fix real failures",
        "prompt": "Fix confirmed real failures with minimal changes. For flaky tests, propose stabilization (retries, isolation, mocks)."
      },
      {
        "name": "Confirm stability",
        "prompt": "Re-run the suite multiple times to confirm real failures are gone and flakiness is reduced or documented.",
        "shellCheck": "npm test -- --testPathPattern=<failing-suite>"
      }
    ],
    "exitCondition": {
      "type": "manual",
      "description": "All failures classified; real regressions fixed; flaky tests documented or stabilized"
    },
    "kickoffPrompt": "Start the \"Flaky Test Triage\" loop.\n\nGoal: classify failing tests as flaky vs real and fix only real regressions\nMax iterations: 5\nBetween iterations run: npm test -- --testPathPattern=<failing-suite>\nExit when: every failure is classified and real regressions are fixed or explicitly deferred\n\nStep 1: Run the failing suite multiple times. Classify each failure, fix real ones, and document flaky behavior.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.",
    "installBundle": {
      "files": []
    },
    "compatibleAgents": [
      "cursor",
      "claude-code"
    ],
    "bestForAgents": [
      "claude-code",
      "cursor"
    ],
    "author": "loops!",
    "authorSlug": "loops",
    "authorOfficial": true,
    "source": "curated",
    "tags": [
      "testing",
      "flaky",
      "triage"
    ],
    "guardrails": [],
    "examples": [],
    "copies": 1065,
    "installs": 1068,
    "views": 1978,
    "featured": false,
    "isPublished": true,
    "createdAt": "Jun 14, 2026"
  }
}

{
  "sourceUrl": "https://loops.elorm.xyz/loops/coverage-until-threshold",
  "downloadedCompatibleBundlePath": ".cursor/loops/coverage-until-threshold/",
  "effectiveGuardrails": [
    "Do not modify the check command or exit criteria to force success",
    "Do not skip, disable, or bypass checks to pass the exit condition",
    "If stuck after several iterations, stop and report blockers instead of gaming metrics",
    "Do not weaken, delete, or skip tests to make the suite pass",
    "Do not replace real assertions with trivial always-pass tests",
    "Prefer fixing production code over patching tests to go green"
  ],
  "kickoffWithGuardrails": "Start the \"Coverage Until Threshold\" loop.\n\nGoal: coverage meets the target threshold (default 80%) with all tests passing\nMax iterations: 12\nBetween iterations run: npm test -- --coverage\nExit when: coverage threshold is met and tests exit 0\n\nStep 1: Run coverage. Add focused tests for the biggest uncovered gaps, then repeat.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.\n\nGuardrails (do not skip):\n- Do not modify the check command or exit criteria to force success\n- Do not skip, disable, or bypass checks to pass the exit condition\n- If stuck after several iterations, stop and report blockers instead of gaming metrics\n- Do not weaken, delete, or skip tests to make the suite pass\n- Do not replace real assertions with trivial always-pass tests\n- Prefer fixing production code over patching tests to go green",
  "loop": {
    "id": "loop73a2ae2ed8ab36e0ff0eb6b9",
    "slug": "coverage-until-threshold",
    "title": "Coverage Until Threshold",
    "description": "Add focused tests until coverage meets your threshold (e.g. 80%), without changing production behavior unnecessarily.",
    "category": "Testing",
    "triggerType": "manual",
    "triggerConfig": {
      "type": "manual"
    },
    "steps": [
      {
        "name": "Measure coverage",
        "prompt": "Run tests with coverage reporting enabled.",
        "shellCheck": "npm test -- --coverage"
      },
      {
        "name": "Add tests",
        "prompt": "Write the smallest tests needed to cover uncovered branches. No snapshot spam or trivial asserts."
      },
      {
        "name": "Re-measure",
        "prompt": "Re-run coverage and confirm threshold is met.",
        "shellCheck": "npm test -- --coverage"
      }
    ],
    "exitCondition": {
      "type": "tests_pass",
      "command": "npm test -- --coverage",
      "description": "Coverage threshold met and all tests pass",
      "maxIterations": 12
    },
    "kickoffPrompt": "Start the \"Coverage Until Threshold\" loop.\n\nGoal: coverage meets the target threshold (default 80%) with all tests passing\nMax iterations: 12\nBetween iterations run: npm test -- --coverage\nExit when: coverage threshold is met and tests exit 0\n\nStep 1: Run coverage. Add focused tests for the biggest uncovered gaps, then repeat.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.",
    "installBundle": {
      "files": []
    },
    "compatibleAgents": [
      "claude-code",
      "cursor"
    ],
    "bestForAgents": [
      "claude-code"
    ],
    "author": "loops!",
    "authorSlug": "loops",
    "authorOfficial": true,
    "source": "curated",
    "tags": [
      "coverage",
      "testing",
      "quality"
    ],
    "guardrails": [],
    "examples": [],
    "copies": 853,
    "installs": 1862,
    "views": 1840,
    "featured": true,
    "isPublished": true,
    "createdAt": "Jun 14, 2026"
  }
}

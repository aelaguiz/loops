{
  "sourceUrl": "https://loops.elorm.xyz/loops/e2e-until-green",
  "downloadedCompatibleBundlePath": ".cursor/loops/e2e-until-green/",
  "effectiveGuardrails": [
    "Do not modify the check command or exit criteria to force success",
    "Do not skip, disable, or bypass checks to pass the exit condition",
    "If stuck after several iterations, stop and report blockers instead of gaming metrics",
    "Do not weaken, delete, or skip tests to make the suite pass",
    "Do not replace real assertions with trivial always-pass tests",
    "Prefer fixing production code over patching tests to go green"
  ],
  "kickoffWithGuardrails": "Start the \"E2E Until Green\" loop.\n\nGoal: E2E suite passes\nMax iterations: 10\nBetween iterations run: npm run test:e2e\nExit when: E2E command exits 0\n\nStep 1: Run E2E tests. Fix the first failing spec, then repeat.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.\n\nGuardrails (do not skip):\n- Do not modify the check command or exit criteria to force success\n- Do not skip, disable, or bypass checks to pass the exit condition\n- If stuck after several iterations, stop and report blockers instead of gaming metrics\n- Do not weaken, delete, or skip tests to make the suite pass\n- Do not replace real assertions with trivial always-pass tests\n- Prefer fixing production code over patching tests to go green",
  "loop": {
    "id": "loop0c7e408362656ea38e056567",
    "slug": "e2e-until-green",
    "title": "E2E Until Green",
    "description": "Run end-to-end tests, fix UI and integration failures, and loop until the E2E suite passes.",
    "category": "Testing",
    "triggerType": "manual",
    "triggerConfig": {
      "type": "manual"
    },
    "steps": [
      {
        "name": "Run E2E",
        "prompt": "Run the project's E2E test command and capture failures.",
        "shellCheck": "npm run test:e2e"
      },
      {
        "name": "Fix failures",
        "prompt": "Fix the smallest root cause for failing E2E specs. Prefer stable selectors and realistic waits."
      },
      {
        "name": "Re-run E2E",
        "prompt": "Re-run E2E until all specs pass.",
        "shellCheck": "npm run test:e2e"
      }
    ],
    "exitCondition": {
      "type": "tests_pass",
      "command": "npm run test:e2e",
      "description": "E2E suite exits 0",
      "maxIterations": 10
    },
    "kickoffPrompt": "Start the \"E2E Until Green\" loop.\n\nGoal: E2E suite passes\nMax iterations: 10\nBetween iterations run: npm run test:e2e\nExit when: E2E command exits 0\n\nStep 1: Run E2E tests. Fix the first failing spec, then repeat.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.",
    "installBundle": {
      "files": []
    },
    "compatibleAgents": [
      "cursor",
      "claude-code"
    ],
    "bestForAgents": [
      "cursor"
    ],
    "author": "loops!",
    "authorSlug": "loops",
    "authorOfficial": true,
    "source": "curated",
    "tags": [
      "e2e",
      "playwright",
      "testing"
    ],
    "guardrails": [],
    "examples": [],
    "copies": 441,
    "installs": 1071,
    "views": 1592,
    "featured": false,
    "isPublished": true,
    "createdAt": "Jun 14, 2026"
  }
}

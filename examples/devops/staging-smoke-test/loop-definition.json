{
  "sourceUrl": "https://loops.elorm.xyz/loops/staging-smoke-test",
  "downloadedCompatibleBundlePath": ".cursor/loops/staging-smoke-test/",
  "effectiveGuardrails": [
    "Do not modify the check command or exit criteria to force success",
    "Do not skip, disable, or bypass checks to pass the exit condition",
    "If stuck after several iterations, stop and report blockers instead of gaming metrics"
  ],
  "kickoffWithGuardrails": "Start the \"Staging Smoke Test\" loop.\n\nGoal: staging smoke checklist passes\nMax iterations: 6\nBetween iterations run: npm run smoke:staging\nExit when: smoke command exits 0\n\nStep 1: Run the staging smoke checklist. Fix the first failing item, then repeat.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.\n\nGuardrails (do not skip):\n- Do not modify the check command or exit criteria to force success\n- Do not skip, disable, or bypass checks to pass the exit condition\n- If stuck after several iterations, stop and report blockers instead of gaming metrics",
  "loop": {
    "id": "loop28aae05be87bfbc5fe43d6fd",
    "slug": "staging-smoke-test",
    "title": "Staging Smoke Test",
    "description": "Run a fixed smoke checklist against staging after deploy: auth, core flows, and error budgets.",
    "category": "DevOps",
    "triggerType": "manual",
    "triggerConfig": {
      "type": "manual"
    },
    "steps": [
      {
        "name": "Run smoke checklist",
        "prompt": "Execute the staging smoke script or manual checklist (login, critical path, webhooks).",
        "shellCheck": "npm run smoke:staging"
      },
      {
        "name": "Fix failures",
        "prompt": "If smoke fails, trace logs, fix the smallest issue, and redeploy or hotfix as appropriate."
      },
      {
        "name": "Confirm green",
        "prompt": "Re-run smoke until all checklist items pass.",
        "shellCheck": "npm run smoke:staging"
      }
    ],
    "exitCondition": {
      "type": "ci_green",
      "command": "npm run smoke:staging",
      "description": "Staging smoke checklist passes",
      "maxIterations": 6
    },
    "kickoffPrompt": "Start the \"Staging Smoke Test\" loop.\n\nGoal: staging smoke checklist passes\nMax iterations: 6\nBetween iterations run: npm run smoke:staging\nExit when: smoke command exits 0\n\nStep 1: Run the staging smoke checklist. Fix the first failing item, then repeat.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.",
    "installBundle": {
      "files": []
    },
    "compatibleAgents": [
      "cursor",
      "claude-code"
    ],
    "bestForAgents": [
      "cursor"
    ],
    "author": "loops!",
    "authorSlug": "loops",
    "authorOfficial": true,
    "source": "curated",
    "tags": [
      "staging",
      "smoke-test",
      "deploy"
    ],
    "guardrails": [],
    "examples": [],
    "copies": 628,
    "installs": 1682,
    "views": 1439,
    "featured": false,
    "isPublished": true,
    "createdAt": "Jun 14, 2026"
  }
}

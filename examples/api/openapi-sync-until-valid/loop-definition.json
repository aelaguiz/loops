{
  "sourceUrl": "https://loops.elorm.xyz/loops/openapi-sync-until-valid",
  "downloadedCompatibleBundlePath": ".cursor/loops/openapi-sync-until-valid/",
  "effectiveGuardrails": [
    "Do not modify the check command or exit criteria to force success",
    "Do not skip, disable, or bypass checks to pass the exit condition",
    "If stuck after several iterations, stop and report blockers instead of gaming metrics"
  ],
  "kickoffWithGuardrails": "Start the \"OpenAPI Sync Until Valid\" loop.\n\nGoal: openapi.yaml lints clean and matches implemented routes\nMax iterations: 8\nBetween iterations run: npx @redocly/cli lint openapi.yaml\nExit when: OpenAPI lint exits 0\n\nStep 1: Lint openapi.yaml. Fix spec errors and handler drift until lint passes.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.\n\nGuardrails (do not skip):\n- Do not modify the check command or exit criteria to force success\n- Do not skip, disable, or bypass checks to pass the exit condition\n- If stuck after several iterations, stop and report blockers instead of gaming metrics",
  "loop": {
    "id": "loopfe50f8b98a6d66aa192aedca",
    "slug": "openapi-sync-until-valid",
    "title": "OpenAPI Sync Until Valid",
    "description": "Keep openapi.yaml valid and in sync with route handlers — lint the spec and fix drift each pass.",
    "category": "API",
    "triggerType": "manual",
    "triggerConfig": {
      "type": "manual"
    },
    "steps": [
      {
        "name": "Lint OpenAPI",
        "prompt": "Validate openapi.yaml with spectral or swagger-cli. List every error with line numbers.",
        "shellCheck": "npx @redocly/cli lint openapi.yaml"
      },
      {
        "name": "Sync handlers",
        "prompt": "Update route handlers or the spec so documented paths, schemas, and status codes match reality."
      }
    ],
    "exitCondition": {
      "type": "lint_clean",
      "command": "npx @redocly/cli lint openapi.yaml",
      "description": "OpenAPI spec lints with zero errors",
      "maxIterations": 8
    },
    "kickoffPrompt": "Start the \"OpenAPI Sync Until Valid\" loop.\n\nGoal: openapi.yaml lints clean and matches implemented routes\nMax iterations: 8\nBetween iterations run: npx @redocly/cli lint openapi.yaml\nExit when: OpenAPI lint exits 0\n\nStep 1: Lint openapi.yaml. Fix spec errors and handler drift until lint passes.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.",
    "installBundle": {
      "files": []
    },
    "compatibleAgents": [
      "cursor",
      "any"
    ],
    "bestForAgents": [
      "cursor"
    ],
    "author": "loops!",
    "authorSlug": "loops",
    "authorOfficial": true,
    "source": "curated",
    "tags": [
      "api",
      "openapi",
      "docs"
    ],
    "guardrails": [],
    "examples": [],
    "copies": 635,
    "installs": 1921,
    "views": 1692,
    "featured": false,
    "isPublished": true,
    "createdAt": "Jun 14, 2026"
  }
}

{
  "sourceUrl": "https://loops.elorm.xyz/loops/pr-babysitter",
  "downloadedCompatibleBundlePath": ".cursor/loops/pr-babysitter/",
  "effectiveGuardrails": [
    "Do not modify the check command or exit criteria to force success",
    "Do not skip, disable, or bypass checks to pass the exit condition",
    "If stuck after several iterations, stop and report blockers instead of gaming metrics"
  ],
  "kickoffWithGuardrails": "/loop 15m Start the \"PR Babysitter\" loop.\n\nGoal: open PRs labeled codex-watch are healthy (CI green, rebased, not stale).\nMax iterations: 20.\nBetween iterations run: gh pr list --label \"codex-watch\"\nExit when: each watched PR is green and current, or escalated.\n\nStep 1: List watched PRs. Fix CI once, rebase if behind, comment if stale. Escalate repeated failures.\n\nGuardrails (do not skip):\n- Do not modify the check command or exit criteria to force success\n- Do not skip, disable, or bypass checks to pass the exit condition\n- If stuck after several iterations, stop and report blockers instead of gaming metrics",
  "loop": {
    "id": "loopd370520f7d1d2ecf46a8ce8e",
    "slug": "pr-babysitter",
    "title": "PR Babysitter",
    "description": "Every 15 minutes, inspect open PRs with a watch label: nudge reviewers, fix CI, rebase if behind main.",
    "category": "CI",
    "triggerType": "interval",
    "triggerConfig": {
      "interval": "15m"
    },
    "steps": [
      {
        "name": "List watched PRs",
        "prompt": "List open PRs with the watch label. Skip merged, draft, or conflict-only PRs that need human product input.",
        "shellCheck": "gh pr list --label \"codex-watch\" --json number,title,mergeable,statusCheckRollup"
      },
      {
        "name": "Triage each PR",
        "prompt": "For each PR: if CI failed, fix once; if behind main, rebase; if stale review, leave a short status comment."
      },
      {
        "name": "Escalate blockers",
        "prompt": "Stop on merge conflicts needing product decisions or the same failure twice. Report blockers clearly."
      }
    ],
    "exitCondition": {
      "type": "manual",
      "description": "All watched PRs are green, rebased, or escalated with a clear blocker note",
      "maxIterations": 20
    },
    "kickoffPrompt": "/loop 15m Start the \"PR Babysitter\" loop.\n\nGoal: open PRs labeled codex-watch are healthy (CI green, rebased, not stale).\nMax iterations: 20.\nBetween iterations run: gh pr list --label \"codex-watch\"\nExit when: each watched PR is green and current, or escalated.\n\nStep 1: List watched PRs. Fix CI once, rebase if behind, comment if stale. Escalate repeated failures.",
    "installBundle": {
      "files": []
    },
    "compatibleAgents": [
      "codex",
      "cursor"
    ],
    "bestForAgents": [
      "codex",
      "cursor"
    ],
    "author": "loops!",
    "authorSlug": "loops",
    "authorOfficial": true,
    "source": "curated",
    "tags": [
      "pr",
      "github",
      "ci",
      "boris-cherny"
    ],
    "guardrails": [],
    "examples": [],
    "copies": 566,
    "installs": 1780,
    "views": 1235,
    "featured": true,
    "isPublished": true,
    "createdAt": "Jun 14, 2026"
  }
}

{
  "sourceUrl": "https://loops.elorm.xyz/loops/ship-pr-until-green",
  "downloadedCompatibleBundlePath": ".cursor/loops/ship-pr-until-green/",
  "effectiveGuardrails": [
    "Do not modify the check command or exit criteria to force success",
    "Do not skip, disable, or bypass checks to pass the exit condition",
    "If stuck after several iterations, stop and report blockers instead of gaming metrics"
  ],
  "kickoffWithGuardrails": "Start the \"Ship PR Until Green\" loop.\n\nGoal: PR is open with all CI checks passing\nMax iterations: 10\nBetween iterations run: gh pr checks\nExit when: all PR checks are success\n\nStep 1: Implement the change, test locally, push, open PR, and fix CI until green.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.\n\nGuardrails (do not skip):\n- Do not modify the check command or exit criteria to force success\n- Do not skip, disable, or bypass checks to pass the exit condition\n- If stuck after several iterations, stop and report blockers instead of gaming metrics",
  "loop": {
    "id": "loop1f2e3ed7d4116c5d41e2604a",
    "slug": "ship-pr-until-green",
    "title": "Ship PR Until Green",
    "description": "Implement on a branch, run tests, push, open a PR, wait for CI, and loop until checks pass and the PR is ready to merge.",
    "category": "CI",
    "triggerType": "manual",
    "triggerConfig": {
      "type": "manual"
    },
    "steps": [
      {
        "name": "Implement and test",
        "prompt": "Make the scoped change and run local tests.",
        "shellCheck": "npm test"
      },
      {
        "name": "Commit and push",
        "prompt": "Commit with a clear message and push the branch."
      },
      {
        "name": "Open or update PR",
        "prompt": "Open a PR with summary and test plan, or update existing PR.",
        "shellCheck": "gh pr view --json statusCheckRollup,url"
      },
      {
        "name": "Fix CI",
        "prompt": "If CI fails, read logs, fix once locally, push, and re-wait."
      }
    ],
    "exitCondition": {
      "type": "ci_green",
      "command": "gh pr checks --watch",
      "description": "PR checks pass and PR is merge-ready",
      "maxIterations": 10
    },
    "kickoffPrompt": "Start the \"Ship PR Until Green\" loop.\n\nGoal: PR is open with all CI checks passing\nMax iterations: 10\nBetween iterations run: gh pr checks\nExit when: all PR checks are success\n\nStep 1: Implement the change, test locally, push, open PR, and fix CI until green.\n\nSelf-pace this loop. After each iteration, run the check command, read the output, and only continue if the exit condition is not met. Stop when the exit condition passes or max iterations is reached. Give a short status update each pass.",
    "installBundle": {
      "files": []
    },
    "compatibleAgents": [
      "claude-code",
      "cursor",
      "codex"
    ],
    "bestForAgents": [
      "claude-code",
      "cursor"
    ],
    "author": "loops!",
    "authorSlug": "loops",
    "authorOfficial": true,
    "source": "curated",
    "tags": [
      "pr",
      "ci",
      "ship",
      "continuous-claude"
    ],
    "guardrails": [],
    "examples": [],
    "copies": 1640,
    "installs": 1928,
    "views": 2826,
    "featured": true,
    "isPublished": true,
    "createdAt": "Jun 14, 2026"
  }
}
